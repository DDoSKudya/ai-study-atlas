# Lychee configuration for markdown link checking.
# Compatible with lychee v0.23.x.

verbose = "info"
no_progress = true
max_retries = 2
accept = [200, 429]
include_mail = false

# Ignore local anchors and repository-internal relative links.
exclude = [
  "^#",
  "^\\./",
  "^\\.\\./",
]
